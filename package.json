{
  "name": "context-ui-workspace",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "build": "pnpm -r --filter @context_ui/* build",
    "dev": "pnpm --filter @context_ui/theme build:css && pnpm -r --parallel --filter @context_ui/theme --filter @context_ui/example-react dev",
    "check": "pnpm -r check",
    "test": "pnpm -r --filter @context_ui/* test"
  }
}
